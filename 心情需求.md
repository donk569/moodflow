# MoodFlow - 状态文档

> 更新日期：2026-06-04 | 版本：v1.0.0 MVP

---

## 一、技术栈

| 层级 | 选择 |
|------|------|
| 框架 | Flutter 3.38.6 + Dart 3.10.7 |
| 状态管理 | Riverpod (StateNotifier + FutureProvider) |
| 本地数据库 | Drift (SQLite) + desugaring |
| 路由 | go_router + StatefulShellRoute (4 Tab) + 翻页动画 |
| AI | DeepSeek API / OpenAI API（用户自填 Key） |
| 安全存储 | flutter_secure_storage + SharedPreferences（双层） |
| 语音 | record ^6.2.1（录制）/ audioplayers（播放） |
| 通知 | flutter_local_notifications + timezone |
| 平台 | Android（iOS 待适配） |

---

## 二、已实现功能 ✅

### 情绪选择
- 3×3 表情网格（😊😰😢😫😤😶😌😐🤩）
- 选中后红色圆圈动画
- 可自由切换，不限次数
- 强度滑杆 1-10
- 性别区分：男生天蓝底🐶 / 女生粉底🐱

### 活动推荐
- **AI 模式**（已配置 API Key）：由 DeepSeek/OpenAI 生成，每次不同
- **规则模式**（未配置 API Key）：每种情绪 6-8 条内置推荐
- 收藏 / 不感兴趣（隐藏） / 最低行动模式（可来回切换）
- 收藏持久化到数据库
- 自定义活动输入
- 翻页动画 + 按钮触感反馈 + 音效

### 完成记录
- 照片上传（相册，最多 4 张）
- 文本输入（可选，AI 辅助生成日记文本）
- 语音录制（最长 60 秒）
- 三选一：✅ 完成记录 / 📝 只打卡 / 🗑️ 跳过不打卡
- 支持再次编辑已有记录

### 打卡 & 连续天数
- 每日自动打卡
- 连续天数统计（实时显示在首页和我的页面）

### 情绪日历
- 月视图，每天显示当日主要情绪表情
- 点击查看：主要情绪 + 平均强度 + 记录次数
- 数据变更实时刷新
- 月份切换

### 历史记录
- 时间线展示，每条显示时间 (HH:mm)
- 按情绪/日期关键词搜索
- 点击展开详情：照片、录音回放、文字、AI 总结
- 批量选择 + 批量删除（二次确认）
- 录音支持反复播放（stop + setSourceDeviceFile + resume 机制）

### 数据统计
- 可选统计周期（7/14/30 天）
- 核心指标：期间记录数、连续打卡、主要情绪、平均强度
- 今日情绪分布柱状图
- 周期趋势图（每日记录数）

### 个人中心
- 用户信息卡片（角色 + 陪伴天数 + 使用统计）
- 收藏活动列表（支持取消收藏）
- 更改个人信息（重新引导）
- 数据统计入口 / AI 设置 / 通用设置 / 云同步入口

### AI 设置
- 支持 DeepSeek 和 OpenAI 双提供商
- API Key 双存储（SecureStorage + SharedPreferences）
- 测试连接按钮（POST /chat/completions）
- 输入即启用保存按钮
- JSON 智能解析（支持 markdown 包裹格式）

### 通用设置
- 深色 / 浅色模式切换（默认浅色）
- 自定义背景图片 + 透明度滑杆（50-255）
- 字体风格选择
- CSV 导出到下载文件夹
- 关于页面（小胡 App 品牌信息、功能介绍、法律信息）

### App 全局
- 杏色暖色调 + 温柔圆角 UI
- 深色 / 浅色主题切换
- 自定义背景图 + 透明度（所有 Tab 页共用）
- 翻书效果页面切换（CustomTransitionPage + 3D rotateY）
- 按钮触感反馈（HapticFeedback）+ 系统点击音效
- 状态保持（后台切回不丢失，MaterialApp.router 全局统一）

### 引导流程
- 3 步引导：欢迎 → 昵称 → 性别/角色选择
- 支持重新引导（更改信息时）
- 昵称实时校验

### 通知
- 每日提醒（自定义时间）
- TZDateTime + `Asia/Shanghai` 时区处理
- 支持通知跳转回 App

### 安全 & 网络
- Android 网络安全配置（允许 cleartext 到 AI API）
- 阿里云 Maven 镜像（中国区构建加速）
- INTERNET 权限 + 后台联网

---

## 三、待实现 📋

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 云同步 (Supabase) | 低 | 本地已完备，云端备份待开发 |
| Lottie 动画 | 低 | 打卡成功等庆祝动画 |
| 周报/月报推送 | 低 | 数据统计已就绪，推送待开发 |
| iOS 适配 | 低 | 当前仅 Android |
| 多语言 | 低 | 当前仅中文 |

---

## 四、项目结构

```
lib/
├── main.dart                          # 入口
├── app.dart                           # MaterialApp.router + 背景 + 主题
├── core/
│   ├── config/app_constants.dart      # 情绪、颜色、性别配置
│   ├── database/
│   │   ├── tables.dart                # 6 张表定义
│   │   ├── database.dart              # Drift 数据库
│   │   └── dao.dart                   # 数据访问扩展
│   ├── theme/app_theme.dart           # 暖白/杏色主题 + 深色模式
│   ├── router/app_router.dart         # go_router 4 Tab + 翻页动画
│   ├── providers/core_providers.dart  # 全局 Provider
│   ├── services/
│   │   └── notification_service.dart  # 每日提醒通知
│   └── utils/beijing_time.dart        # 北京时间工具
├── features/
│   ├── emotion/                       # 情绪选择 + 首页
│   ├── recommend/                     # 推荐引擎 (规则 + AI)
│   │   ├── domain/recommend_provider.dart
│   │   ├── engine/recommend_engine.dart
│   │   └── presentation/
│   │       ├── recommend_page.dart
│   │       └── widgets/activity_card.dart
│   ├── record/                        # 记录 + 打卡
│   ├── calendar/                      # 情绪日历
│   ├── history/                       # 历史时间线
│   ├── profile/                       # 个人中心/设置/统计/关于
│   └── onboard/                       # 引导页
├── ai/
│   ├── ai_config_service.dart         # Key 管理 + 提供商
│   └── openai_service.dart            # API 调用 + JSON 解析
└── shared/widgets/                    # 通用组件
```

---

## 五、数据库 (Drift 6 表)

- `users` — 用户信息
- `mood_records` — 心情记录
- `activity_logs` — 活动记录
- `diary_entries` — 日记（文字/图片/语音/AI总结）
- `daily_checkins` — 每日打卡
- `recommendation_prefs` — 推荐偏好（收藏/隐藏）

---

## 六、数据流

```
记录心情 → dataVersionProvider++ → 历史/日历/打卡实时刷新
AI 推荐 → 后台异步 → 追加到推荐列表
收藏 → 写入 recommendation_prefs 表 → 个人中心可查看
背景/主题 → SharedPreferences → App 启动时读取
API Key → SecureStorage + SharedPreferences → AI 服务调用
```
